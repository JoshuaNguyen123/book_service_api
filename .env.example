# Optional: override database path (default: sqlite:///./data/app.db)
# DATABASE_URL=sqlite:///./data/app.db

# Optional: enable CORS (comma-separated origins, e.g. http://localhost:3000)
# CORS_ORIGINS=http://localhost:3000
